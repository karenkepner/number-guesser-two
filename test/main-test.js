var assert = require('chai').assert;
var generateRandomNumber = require('../js/main');

describe('generateRandomNumber', function(){

	it('should be a function', function() {
	assert.isFunction(generateRandomNumber);
	}


});
